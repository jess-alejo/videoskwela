!!!
%html{ lang: "en", "data-footer"=>"true",
  "data-override" => "{\"attributes\": { \"layout\": \"boxed\" } }"
  }

  = render "layouts/head"

  %body{ "data-bs-padding" => "15px", :style => ""}
    = render "layouts/modal"

    #root
      = render "layouts/nav"

      %main
        = yield

      = render "layouts/footer"

    = render "layouts/scripts"
